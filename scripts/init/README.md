# Initialization Scripts

Target location for future database initialization scripts.

Current file kept at repository root for compatibility:

- `init-db.sql`

Do not move it until the related Docker Compose references are updated in a dedicated functional task.
