# Migration Helper Scripts

Target location for future migration helper scripts.

Current files kept at repository root for compatibility:

- `migrate.ps1`
- `migrate.sh`

Do not move them until their project-root detection and documentation are updated in a dedicated task.
