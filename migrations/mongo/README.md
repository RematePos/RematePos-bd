# Mongo Migrations

Target folder for future Mongo migration organization.

Current Mongo migration files remain at:

- `mongo/changelog/`
- `mongo/run-migrations.sh`

The current Docker Compose file mounts these paths directly, so they are not moved in HU-094.
